const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "db4free.net",
    user: "prince",
    password: "gospel333",
    database: "calculator",
  },
  listPerPage: 10,
};
module.exports = config;
